#!/usr/bin/env python3

import sys
from decimal import Decimal


total = Decimal(0)
count = 0

for line in sys.stdin:

    number = Decimal(line)

    total += number
    count += 1

print(f"Count: {count}  Total: {total}  Average: {total / count}")
